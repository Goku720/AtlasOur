import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  allowedDevOrigins: ['192.168.8.100', '*.ngrok-free.app', 'rigid-creation-catacomb.ngrok-free.dev'],
};

export default nextConfig;
